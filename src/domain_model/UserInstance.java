package domain_model;

//Enum class containing value for each type of Member where an instance can be created of that member type.
public enum UserInstance {
    MEMBER,
    COMPETITIVE,
    TRAINER,
    SUPER,
    TREASURER,
    NOTFOUND;
}
